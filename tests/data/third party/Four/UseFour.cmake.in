# Include project include directories
INCLUDE_DIRECTORIES(${Four_INCLUDE_DIRS})

# Include project library directories
LINK_DIRECTORIES(${Four_LIBRARY_DIRS})

# Include project library directories
LINK_LIBRARIES(${Four_LIBRARIES})
